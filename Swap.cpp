#include<iostream>
#include<SLL.h>

template<class T>

void SLList<T>::swap(T val1,T val2)
{
    SLLNode<T> *prev1 = nullptr, *prev2 = nullptr; *ptr1 = nullptr; *ptr2 = nullptr;
    for(SLLNode<T>*prev = nullptr, *ptr = head; ptr2 == nullptr && ptr != nullptr;

}
